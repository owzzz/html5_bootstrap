language: node_js
node_js:
  - "0.10"
before_install:
  - brew update
  - brew install GraphicsMagick
  - brew install phantomjs
  - bundle install

install:
  - npm install -g bower
  - npm install -g napa
  - npm install
  - bower install

script: "grunt dev" 