language: node_js
node_js:
  - "0.10"
before_install:
  - bundle install
  - brew install GraphicsMagick
  - brew install phantomjs

install:
  - npm install -g bower
  - npm install
  - bower install

script: "grunt dev" 